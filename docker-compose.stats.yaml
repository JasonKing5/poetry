services:
  umami:
    image: ghcr.io/umami-software/umami:postgresql-latest
    container_name: umami
    restart: always
    init: true
    ports:
      - "3001:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@poetry-db:5432/umami
      APP_SECRET: umami
    networks:
      - poetry-network

networks:
  poetry-network:
    external: true